allowBuilds:
  esbuild: true
# Docs consumes the library via a local "link:.." dependency, so the
# supply-chain release-age gate (which only matters for freshly published
# npm packages) is unnecessary friction here — disable it.
minimumReleaseAge: 0
